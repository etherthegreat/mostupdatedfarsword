Run `python3 scripts/pick_ethertask.py` from the repo root and display the printed task card exactly as-is, inside a code block. Then in plain text below the card, give a one-sentence description of the art scene (what to draw, the mood, key characters/objects) based on the event name and notes. Do not add any other commentary or instructions — the card is the complete task.

⚠️ NEVER mark a task as FULL PASS / FULL COMPLETION / update status / run the builder unless the user explicitly runs `/closetask`. Do the work; hold the close.
